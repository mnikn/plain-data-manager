import { FormInstance } from "antd";
import { SchemaStringField } from "models/schema/string";

const StringField = ({ data, form }: { data: SchemaStringField, form: FormInstance }) => {
  return <div>dd</div>;
};

export default StringField;
